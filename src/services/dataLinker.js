// AdPilot — Cross-report data linker
// Joins STR + Placement + Advertised Products on Campaign Name / Ad Group Name
// Checkpoint 3 will implement full logic — stub for now.

export const linkReports = (strData, placementData, productData) => {
  // TODO: Checkpoint 3
  return { strData, placementData, productData }
}
